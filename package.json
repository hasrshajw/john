{
  "name": "text-to-json-app",
  "version": "1.0.0",
  "description": "Extracts structured JSON from text using the Gemini API.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.11.4"
  }

}
